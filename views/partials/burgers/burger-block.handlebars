<li>
	{{name}} {{price}}

	<button class="but change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}Devour!{{else}}Reorder!{{/if}}
	</button>

	<button class="but delete-burger fa fa-trash " data-id="{{this.id}}"></button>
</li>
